import { ProjectOnboardingClient } from "@/components/project/ProjectOnboardingClient";

export const metadata = { title: "Build with Moonshill" };

export default function BuildPage() {
  return <ProjectOnboardingClient />;
}
