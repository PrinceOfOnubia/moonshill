import { LeaderboardClient } from "@/components/leaderboard/LeaderboardClient";

export const metadata = { title: "Leaderboard — Memebook" };

export default function LeaderboardPage() {
  return <LeaderboardClient />;
}
