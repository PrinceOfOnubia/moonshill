import { CreateClient } from "@/components/create/CreateClient";

export const metadata = { title: "Create a challenge — Memebook" };

export default function CreatePage() {
  return <CreateClient />;
}
